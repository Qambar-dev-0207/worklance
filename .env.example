# Database Configuration (MongoDB Local or MongoDB Atlas Cluster)
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/worklance?retryWrites=true&w=majority

# Security & Authentication (Required for Production JWT signing)
JWT_SECRET=your_production_secret_jwt_key_min_32_characters

# Application Configuration
NEXT_PUBLIC_APP_URL=https://worklance.vercel.app
NODE_ENV=production
